let style = "color: yellow";
console.log("%c==== Use of `var` Keyword ====", style);

/** ====================================================================
 * var
 * Can be declared without assigning a value
 * Can be assigned a value later
 * Can be re-assigned a value
 * Can be declared and assigned at a time
 * Can be re-declared
 */

var myAge;
myAge = 30;
myAge = 35;
var myAge = 45;
myAge = 48;

console.log(myAge);
